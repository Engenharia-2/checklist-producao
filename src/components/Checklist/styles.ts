import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
     container: {
        padding: 16,
        paddingBottom: 32,
    },
    imageStepContainer: {
        flex: 1,
        padding: 16,
        paddingBottom: 32,
    }
})
